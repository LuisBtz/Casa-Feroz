import React from "react";
import styled from "styled-components";

const Hero = () => {
    return(
        <HeroContainer>
            <h1>Hero</h1>
        </HeroContainer>
    )
}

const HeroContainer = styled.section`

`

export default Hero